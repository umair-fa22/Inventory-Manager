# Application Configuration
PORT=3000
APP_PORT=3000

# MongoDB Configuration
MONGO_USERNAME=admin
MONGO_PASSWORD=your_secure_mongo_password_here
MONGO_PORT=27017
MONGODB_URI=mongodb://admin:your_secure_mongo_password_here@mongo:27017/
DATABASE=inventory_db
COLLECTION=items

# Redis Configuration
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_secure_redis_password_here
CACHE_TTL=300

# Legacy MongoDB Variables (if using MongoDB Atlas)
# MONGOUSR=your_username
# MONGOPASS=your_password
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all placeholder passwords with strong, randomly generated passwords
# 3. Never commit .env file to version control (already in .gitignore)
# 4. For production, use environment variables or secrets management systems (Kubernetes Secrets, AWS Secrets Manager, etc.)

